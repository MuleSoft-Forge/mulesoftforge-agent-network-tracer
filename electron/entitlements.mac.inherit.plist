<?xml version="1.0" encoding="UTF-8"?>
<!--
  Entitlements for Electron's HELPER processes (renderer, GPU, plugin).

  These are spawned by the main app, so com.apple.security.inherit is correct
  here — it is invalid on the main bundle (see entitlements.mac.plist).
-->
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.inherit</key>
    <true/>
  </dict>
</plist>
