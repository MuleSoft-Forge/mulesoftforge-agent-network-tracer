import type { HelpImage } from "@/lib/composer/help/help-catalog";

/** Exchange asset detail page — where network title and yaml summary appear after publish. */
export const EXCHANGE_NETWORK_LISTING_SCREENSHOT: HelpImage = {
  src: "/images/help/exchange-network-listing.png",
  alt: "Exchange asset page with network title and YAML summary subtitle highlighted",
};
